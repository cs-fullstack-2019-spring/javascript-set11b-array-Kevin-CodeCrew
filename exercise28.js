// 1. Create an array.
// 2. Add four names to the array.
// 3. Print the array
/*
names are inside the array
loging the entire names into the array.
push puts names at the end
pull takes from the end
unshift puts at the start
shift pulls from the start
 */


var names = ['kenn','kevin','erin','meka'];

names.push('thomas','shoshard','joshua','chelsa');

console.log(names);